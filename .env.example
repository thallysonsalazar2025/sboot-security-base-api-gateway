# Copy to .env locally and provide a non-production HS512 secret (minimum 64 bytes).
# Never commit real credentials or production secrets.
JWT_HS512_SECRET=replace-with-local-test-secret-at-least-64-bytes-long-0000000000000000
LOG_ELASTICSEARCH_ENABLED=false
