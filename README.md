# sboot-security-base-api-gateway
sboot-security-base-api-gateway is a reusable Spring Boot API Gateway built with Spring Cloud Gateway and Spring Security. It acts as the secure entry point for client applications, validating JWT tokens issued by an Authorization Server, enforcing access control, and routing requests to backend microservices in a scalable architecture.
